const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      backgroundImage: {
        landing: "url('src/assets/images/landingImage.jpg')",
      },
    },
  },
};

export default config;
